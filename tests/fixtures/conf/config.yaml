defaults:
  - generic/model: convlstm
  - generic/dataset: psqlseqsatos

generic:
  seed: 1
  verbose: False
  runner_name: ???
  lr: 1e-3
  optimizer: adam
  scheduler:
  batch_size: 2 # Data
trainer:
  fast_dev_run: True
  default_root_dir: ???
  logger: False
other:
  save_top_k: 0
