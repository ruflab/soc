defaults:
  - generic/model: convlstm
  - generic/dataset: psqlseqsatos
  - generic/val_dataset: null

generic:
  seed: 1
  verbose: False
  runner_name: ???
  lr: 1.
  optimizer: adam
  scheduler:
  batch_size: 4 # Data
trainer:
  default_root_dir: ???
  logger: False
  fast_dev_run: True
  max_steps: null
  max_epochs: 1
other:
  save_top_k: 0
