defaults:
  - generic/model: convlstm
  - generic/dataset: psqlseqsatos

generic:
  seed: 1
  verbose: False
  runner_name: ???
  lr: 1.
  optimizer: adam
  scheduler:
  batch_size: 2 # Data
trainer:
  default_root_dir: ???
  logger: False
  max_steps: 2
other:
  save_top_k: 0
