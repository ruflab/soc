# @package _global_
generic:
  seed: 1
  verbose: False
  dataset: SocPSQLSeqSAToSDataset
  data_input_size: ???
  data_output_size: ???
  model: ConvLSTM
  h_chan_dim: [150, 150]
  kernel_size: [[3, 3], [3, 3]]
  strides: [[3, 3], [3, 3]]
  num_layers: 2
  lr: 1e-3
  optimizer: adam
  scheduler:
  batch_size: 2 # Data
  no_db: True # Model
trainer:
  fast_dev_run: True
  default_root_dir: self.folder
  logger: False
other:
  save_top_k: 0
